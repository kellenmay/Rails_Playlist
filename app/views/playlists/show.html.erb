<h1>Playlists Show Page</h1>

<p> <%= @playlist.name %></p>
Songs'

<p>Title: </p>
<br>

<% @playlist.songs.each do |song| %>
    <p>
        Title:<%= song.title %>
        <br>
        Artist:<%= song.artist %> 
    </p>
<% end %>




<%= button_to "Delete", @playlist, method: :delete %>



<%= render partial: "users/buttons" %>
