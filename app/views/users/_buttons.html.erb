<header>


<% if @playlist %>
   <% if @playlist.creator_id == current_user.id %> 
        <%= button_to "Add Songs to Playlist", new_playlist_playlist_song_path(@playlist), method: :get %>
        <%= button_to "Delete", @playlist, method: :delete %>
    <% end%>
<% end%>
<% if current_user %>
<%= button_to "Profile",    '/profile', method: :get %>
<% end %>
<%= button_to "Create New Song", '/songs/new', method: :get %>

<%= button_to "Create New Playlist", '/playlists/new', method: :get %>

<%= button_to "Songs",    songs_path, method: :get %>

<%= button_to "Playlists", playlists_path, method: :get %>


    
</header>